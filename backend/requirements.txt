# ─────────────────────────────────────────────────────────────────
# NOMO-WORKCHAIN Backend — requirements.txt
# Python 3.11+
# Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────

# ── Web framework ─────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0      # ASGI server with hot reload
requests==2.31.0               # Sync HTTP client (verification services)


# ── Blockchain ────────────────────────────────────────────────────
web3==6.18.0                   # Monad is EVM-compatible — web3.py works directly
eth-account==0.11.0            # Sign transactions with backend verifier wallet
eth-abi==5.0.1                 # ABI encode/decode for contract calls

# ── Database ──────────────────────────────────────────────────────
sqlalchemy==2.0.30             # ORM for verification records
alembic==1.13.1                # Database migrations
asyncpg==0.29.0                # Async PostgreSQL driver (production)
aiosqlite==0.20.0              # Async SQLite driver (local development)

# ── Async / background jobs ───────────────────────────────────────
celery==5.4.0                  # Task queue for verification workers
redis==5.0.4                   # Celery broker + result backend
aiohttp==3.9.5                 # Async HTTP for GitHub API + link verification

# ── Verification: GitHub ──────────────────────────────────────────
PyGithub==2.3.0                # GitHub REST API — verify commits, PRs, repos

# ── Verification: File hash ───────────────────────────────────────
# Standard library hashlib covers SHA256 — no extra dep needed

# ── Verification: IPFS ───────────────────────────────────────────
ipfshttpclient==0.8.0a2        # Fetch and verify files pinned on IPFS

# ── Config and environment ────────────────────────────────────────
pydantic==2.7.1                # Data validation (FastAPI native)
pydantic-settings==2.2.1       # Settings from .env file
python-dotenv==1.0.1           # Load .env for local dev

# ── Security ─────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT tokens if auth is added later
passlib[bcrypt]==1.7.4             # Password hashing (future auth)
cryptography==42.0.7               # TLS + key operations

# ── HTTP / API ────────────────────────────────────────────────────
httpx==0.27.0                  # Async HTTP client (used in tests + link verification)
python-multipart==0.0.9        # File upload support (file hash verification)

# ── Logging and monitoring ────────────────────────────────────────
structlog==24.1.0              # Structured JSON logging
sentry-sdk[fastapi]==1.45.0    # Error tracking (optional — remove if not using Sentry)

# ── Testing ───────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0
httpx==0.27.0                  # Test client for FastAPI

# ── Dev tooling ───────────────────────────────────────────────────
black==24.4.2                  # Formatter
isort==5.13.2                  # Import sorter
ruff==0.4.4                    # Linter
